<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dalyexpencemanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DalyExpenceManager">
        <activity android:name=".InstagramActivity"></activity>
        <activity android:name=".AfganistanActivity" />
        <activity android:name=".EmailActivity" />
        <activity android:name=".FacebookActivity" />
        <activity android:name=".CurrencyActivity" />
        <activity android:name=".ExpensesView" />
        <activity android:name=".IncomeView" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".ExpenseActivity" />
        <activity android:name=".IncomeActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>